import Article from "../models/article.model.js";
import User from "../models/user.model.js";

// for admin only

// totoal users per month
// total reporter per month 
// total \published articles per month
// total accepetd , rejected and draft per month

const getAdminStats = () => {};

export { getAdminStats };
